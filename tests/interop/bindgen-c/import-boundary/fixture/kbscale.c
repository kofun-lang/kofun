/* kbscale — the fixture library the import-boundary gate links (#1217). */
#include "kbscale.h"

long kbscale_double(long value) {
    return value * 2;
}

long kbscale_add(long left, long right) {
    return left + right;
}
