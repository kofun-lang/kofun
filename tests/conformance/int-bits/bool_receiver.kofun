# Boolean literals are primaries, but bit methods belong only to Int. The bad
# receiver is diagnosed before the labelled argument is considered.

fn main() -> Int {
    print(to_text(true.and(value: 1)))
    return 0
}
