# #1517. A read-only destination may not reach clear.
fn misuse(read destination: Bytes) -> Int {
    stage2_bytes_clear(destination)
    return 0
}

fn main() -> Int {
    let owned = stage2_bytes_empty()
    return misuse(owned)
}
