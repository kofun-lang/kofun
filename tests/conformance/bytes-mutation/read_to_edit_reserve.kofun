# #1517. A read-only destination may not reach reserve.
fn misuse(read destination: Bytes) -> Int {
    stage2_bytes_reserve(destination, 16)
    return 0
}

fn main() -> Int {
    let owned = stage2_bytes_empty()
    return misuse(owned)
}
