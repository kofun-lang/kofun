# #1517. A read-only destination may not reach append.
fn misuse(read destination: Bytes) -> Int {
    stage2_bytes_append(destination, 1)
    return 0
}

fn main() -> Int {
    let owned = stage2_bytes_empty()
    return misuse(owned)
}
