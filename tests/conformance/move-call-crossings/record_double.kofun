type Point = { x: Int, valid: Bool }

fn consume(take point: Point) -> Int {
    return point.x
}

fn main() -> Int {
    let point: Point = Point(x: 7, valid: true)
    print(consume(point))
    print(consume(point))
    return 0
}
