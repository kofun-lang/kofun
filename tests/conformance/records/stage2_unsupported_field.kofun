# The record-field boundary, held by whatever is currently outside it.
#
# It was `text: Text` until #1181 admitted `Text`, and it moved rather than
# being deleted: what this fixture proves is that a field type outside the
# slice is refused before any C is written, not that any particular type is.
# `List[Int]` is the next one, and #1181's sibling increment.

type Unsupported = {
    samples: List[Int],
}

fn main() -> Int {
    return 0
}
