# diagnostic-mode: compile
# expect-code: E2S15
# expect-span: byte 322
# #1658, with #1243's `owned_record_equality_int`. An authority-bearing
# record is refused by the general record rule; #1243 may order E353
# ahead of it later.
type Holder = { slot: RootAuthority }
fn same(read value: Holder) -> Int {
    if value == value {
        return 1
    }
    return 0
}
fn main() -> Int {
    return 0
}
