# diagnostic-mode: compile
# expect-code: E353
# expect-span: byte 123
fn holder(read root: RootAuthority) -> Int {
    if root == root {
        return 1
    }
    return 0
}

fn main() -> Int {
    return 0
}
