# diagnostic-mode: compile
# expect-code: E353
# expect-span: byte 163
type Holder = { slot: RootAuthority }

fn leak(read value: Holder) -> Int {
    let inner = value.slot
    return 0
}

fn main() -> Int {
    return 0
}
