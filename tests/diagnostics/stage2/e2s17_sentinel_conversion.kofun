# diagnostic-mode: compile
# expect-code: E2S12
# expect-span: byte 439
# #1411. `call_arity` refuses an unparsable argument list with -1, and
# validate_numeric_conversions consumed it as a count -- the author was told their call
# had minus one arguments. The four sites are independent: removing one
# guard regresses only its own reproduction, so each needs its own fixture.
fn g(a: Int) -> Int {
    return a
}

fn main() {
    print(Decimal.from_int(1 == 1))
}
