# Release claims

Generated from `release/claims.json` by `task release-evidence`. Do not edit.

| Claim | State | Area | Capability |
|---|---|---|---|
| `affine-resource-handle` | checkpoint | frontend | One nominal `AffineTransport` implements the closed RFC-0010 read/write/drain/close/cancel table. The standalone record evaluator and emitted Stage 2 C11 agree on the normal observations; production E2S123 checks refuse three moved-handle misuses; and a scripted C11 host boundary refuses stale or adversarially duplicated generations as EARH01. This is not a general ownership pass or environment authority. |
| `arithmetic-core` | implemented | frontend | The arithmetic Core is validated and emitted for the checked Int64 operator set; `/` is deliberately not part of it. |
| `bindgen-c-stage1` | checkpoint | interop | One self-contained C header becomes raw `extern "C"` declarations in the checked C ABI profile plus a machine-readable audit report, deterministically and with the interpretation context pinned by a digest. Sizes, alignments, field offsets, enum values, and the calling convention are verified against the C compiler rather than recorded from the generator's own arithmetic. Constructs outside the profile — macros, variadics, unions, bitfields, flexible arrays, inline functions, explicit calling conventions — are reported with a reason and never bound. Generated code is marked `raw-trusted-foreign` and is not safe: pointer ownership, lifetime, thread affinity, callback duration, and error conventions are recorded as unreviewed review rows, not checked. Arbitrary C is not supported, a safe facade is hand-written reviewed work, and `kofun migrate-c` does not exist. |
| `borrowed-list-ownership` | checkpoint | frontend | Stage 2 rejects use-after-move for borrowed `List` bindings only. It is not a general ownership pass. |
| `bounded-tzdb-producer` | checkpoint | c-backend | One offline fixed-capacity tzdb fixture is parsed and validated in Kofun source, including version/content-digest provenance, deterministic lookup, explicit unique/gap/fold results, typed malformed and limit outcomes, and agreement between the reference executor and emitted C11. It is not a packaged IANA database, ambient file adapter, or stable public API. |
| `c-abi-profile` | checkpoint | interop | `extern` declarations and `repr(C)` layouts are honoured for the host C compiler only; no cross-ABI or cross-target promise is made. |
| `c11-function-calls` | checkpoint | c-backend | User functions lower to C11 for the Int Core, including recursion and forward references. Other value types are outside the slice. |
| `c11-list-int-values` | checkpoint | c-backend | Stage 2 C11 lowers bounded `List[Int]` literals, immutable or mutable locals, direct parameters and results, `len`, checked reads, and standalone mutable-local element writes through one capacity-64 by-value carrier. Reads and writes share positive, negative, constant, and dynamic bounds behavior. The separate `nominal-records` claim owns record fields and field reads; that bounded bridge does not promote this general list family. General `List[T]`, growth, mutation through parameters or immutable bindings, assignment expressions, `List[Text]`, named nested aggregates, indirect calls, and non-C11 backends remain outside the slice. An iterative `while` may drive those reads: the loop condition is re-evaluated each iteration, so a scan over an indexed `List[Int]` executes and terminates (#1128). |
| `checked-int64-contract` | implemented | frontend | Every ordinary Core `Int` arithmetic operation is checked: overflow and non-representable results are refused or trapped, never implicitly wrapped. Explicitly named modular operations are outside this claim. |
| `cli-commands` | checkpoint | tooling | `build`, `run`, `check` and `test` accept Core sources. Sources outside the Core are refused by the driver, not partially compiled. |
| `cli-framework` | checkpoint | frameworks | The CLI framework produces a direct static Linux x86-64 binary for the declarative command profile. No dynamic linking or other host is covered. |
| `compiled-visibility-interfaces` | checkpoint | frontend | The normal Stage 2 compiler publishes committed Int and flat nominal ADT parameter, result, constructor-payload, and external-label/unlabelled-vector identities into canonical public and package-internal KIF views. Public facts may reference only public ADTs; internal facts may reference public or internal ADTs; private and absent identities fail closed. Records, generics, effects, and ownership signature components remain explicit unsupported-publication boundaries. |
| `compiler-seed` | implemented | bootstrap | The Stage 1 seed is written in Kofun and kept beside a hand-written C transliteration. Its line-oriented Core lowers checked Int arithmetic, six comparisons, Bool equality, Bool literals and bindings, unary `!`, short-circuiting `&&`/`||`, Text literals/concatenation/equality, byte-oriented Text/List indexing, nested `if`/`else if`/`else` blocks, and `while`/half-open `for` ranges. The 15 builtins used by the frozen self-host source have exact arity and typed lowering: `args`, `chars`, `contains`, `find`, `is_digit`, `is_space`, `is_xid_continue`, `len`, `print`, `read_text`, `starts_with`, `text_slice`, `trim`, `validate_unicode_source`, and `write_text`; the Core still has no assignment or non-main function declaration. |
| `decimal-arithmetic-v1` | checkpoint | c-backend | Stage 2 C11 lowers exact arbitrary-precision Decimal addition, subtraction, multiplication, comparison and checked exact division, explicit round and rounded divide with mandatory destination scale and one of five modes, and format/parse that never rounds, plus explicitly binary64 Float arithmetic. Int has no `/`, Decimal has no `//` or `%`, mixed numeric arithmetic requires an explicit conversion, and the other declared backends remain unsupported for this corpus. |
| `deterministic-fuzzing` | implemented | quality | Fuzzing is seeded and reproducible: the same seed yields the same programs and the same oracle/backend observations. |
| `diverse-double-compilation` | checkpoint | bootstrap | Two unrelated host C compilers each build a Kofun compiler, and the two emit byte-identical C and agree on every driver corpus case, so a payload carried by one host toolchain cannot survive unnoticed. Both chains run on one machine, one libc, one kernel and one checkout, so a payload below the C compiler is shared by both and is invisible here; independent reproduction by a builder that did not produce the evidence is #274 and remains open. |
| `documentation-index` | checkpoint | tooling | A validated KIF v1.1 visibility projection supplies stable declaration identities, names, signatures, visibility and semantic-view digests; the matching typed sidecar supplies only current validated-identity status. Public output contains only public KIF facts. Package-internal output requires the exact declaring PackageId and labels internal entries non-public. Partial output is a validated prefix, while stale and cancelled projections contain no entries. |
| `elf64-image-writer` | checkpoint | native | The backend writes a static ELF64 image directly, without an external assembler or linker, for the covered Core profiles. |
| `enum-matching` | checkpoint | frontend | Concrete enums match exhaustively over their constructor set. A constructor may declare one `Int` payload field and be constructed as `C(expr)` in an explicitly typed initializer, same-typed function argument, or same-typed return. Enum bindings pass to and return from same-typed functions. Matches accept `C(name)`, `C(_)`, `_`, and a binding catch-all that may be re-matched. Wider payload types, more than one payload field, nested payload patterns, generic enums, and non-C11 backends are outside the slice. |
| `formatter-and-repl` | design | tooling | Design only. No formatter or REPL is shipped, and no gate claims one. |
| `general-native-lowering` | open | native | Unified types, unified control flow, and additional target profiles are absent. Only the enumerated native checkpoints are claimed. |
| `general-ownership-checking` | open | frontend | There is no general ownership or law pass. Only the narrow borrowed-`List` checkpoint is claimed. |
| `general-parser-type-checker` | open | frontend | The active compiler is not a general parser or type checker. Only the enumerated Stage 2 checkpoints are claimed. |
| `http-framework` | checkpoint | frameworks | A bounded HTTP/1.1 server library over epoll, reached through the C ABI profile. TLS, HTTP/2 and client redirects are outside it. |
| `macho64-ad-hoc-signing` | checkpoint | native | The canonical Kofun encoder directly writes the __LINKEDIT segment, LC_CODE_SIGNATURE, embedded SuperBlob, version 0x20400 CodeDirectory, and SHA-256 page hash for deterministic x86-64 and AArch64 Mach-O 64 images. Big-endian fields, SHA-256 compression and padding, bounded ASCII identifier, and every producing value are Kofun-owned; no assembler, linker, system SDK, codesign, system digest, or hand-authored foreign-language image or digest implementation participates. The current bounded execution bridge still translates the Kofun fixture through the existing C bootstrap and supplies no image or digest values; removing that bridge is outside this checkpoint. |
| `macho64-image-writer` | checkpoint | native | The canonical Kofun encoder writes complete page-sized Mach-O 64 images directly for x86-64 and AArch64: exact CPU identities, MH_EXECUTE, __PAGEZERO, one executable __TEXT,__text section, LC_LOAD_DYLINKER, LC_BUILD_VERSION, LC_MAIN, and distinct raw exit entry sequences. No assembler, linker, system SDK, imported library, or foreign-language image writer participates. |
| `module-aliases` | checkpoint | frontend | Same-package `import a.b as local` only. The alias is local, never re-exported, and does not route the driver. |
| `native-aarch64-function-calls` | checkpoint | native | The same bounded Int Core lowers to AArch64. Execution evidence requires `qemu-aarch64`; without it the gate reports a skip rather than a pass. |
| `native-constant-stack-returns` | checkpoint | native | A `return` of a direct call becomes a branch, so recursion in tail position runs in constant stack. Calls in non-return position still consume stack. |
| `native-int64-values` | checkpoint | native | Literals reach `INT64_MAX` and checked expressions reach the complete signed range, with deterministic encodings on both targets. |
| `native-integer-division` | checkpoint | native | `//` and `%` follow floor semantics on both targets. `/` is refused on `Int`, and zero or non-representable quotients trap with a canonical per-operator `R010`. |
| `native-list-int-core` | checkpoint | native | `List[Int]` values lower natively on both targets for the Core operation set. AArch64 execution evidence requires qemu. |
| `native-text-returning-calls` | checkpoint | native | Functions returning `Text` lower on both targets for the compiler-shaped profile: parameters, locals, concatenation, forwarding and direct calls. |
| `native-utf8-text-core` | checkpoint | native | UTF-8 `Text` values lower natively on both targets for the Core operation set, with codepoint and grapheme behaviour gated against Unicode data. |
| `native-x86-64-function-calls` | checkpoint | native | Up to six integer arguments, guarded returns and recursion, for the Int Core only. |
| `nominal-records` | checkpoint | frontend | The dedicated record frontend still accepts the full bounded typed contract. The production Stage 2 C11 slice additionally lowers nominal `Int`/`Bool`/`Text`/capacity-64 `List[Int]` fields, preserves written construction evaluation order, uses declaration-order AggregateLayout, supports typed field reads, passes/returns whole records by value, and copies a list field out of its record carrier. One mixed `BridgeReport` runs twice against exact UTF-8 bytes and independently agrees with AggregateLayout v1. Its typed sidecar separately proves nominal, function, resolved base-reference, and concrete-type facts; it does not publish a selector or field-use fact. Production emitted C member reads and the runtime golden prove field access across that explicit artifact boundary. General lists, `List[Text]`, named nested aggregate fields, module visibility, generic records, native lowering, and a stable ABI remain outside the checkpoint. |
| `pe32plus-image-writer` | checkpoint | native | The canonical Kofun encoder writes complete 1,024-byte PE32+ images directly for x86-64 and AArch64: DOS header and e_lfanew, PE signature, exact COFF machine identity, PE32+ optional header, one aligned executable .text section, and distinct no-import entry sequences. No assembler, linker, system SDK, import library, or foreign-language image writer participates. |
| `public-re-exports` | checkpoint | frontend | Same-package `pub import` / `pub from` with non-widening edges and explicit numeric bounds: 64-edge chains, 1,024 bindings per module, 65,536 edges per package. |
| `reproducible-bootstrap` | implemented | bootstrap | Regenerating Stage 1 from its seed reproduces the checked-in artifact byte for byte on the supported host toolchain. |
| `rust-crate-shim` | checkpoint | interop | One vendored Rust crate is reached through a gated C ABI shim, as a worked example. It is not a general Rust interop story. |
| `self-recompile` | checkpoint | self-hosting | The frozen profile reaches the three-generation semantic fixed point: A1(S) -> C2/A2, A2(S) -> C3/A3, with C2 == C3 and A2 == A3 byte for byte and C1/A1 as hash-pinned runnable provenance. Full-language self-hosting is not claimed. |
| `selfhost-native-corpus` | checkpoint | self-hosting | The driver's five-`print` success corpus reaches a static ELF on both native targets and its output matches the self-host C11 path exactly. |
| `source-extension` | implemented | tooling | `.kofun` is the only source extension in the repository. The editor integration that registers it is hjosugi/kofun-vscode and is gated there, not here. |
| `stable-diagnostics` | implemented | quality | Every diagnostic code has a canonical registry row and an executable family owner. Stage 2 retains 46 of 46 codes, with 3 span debts recorded rather than hidden. |
| `stage2-core-lowering` | checkpoint | frontend | Stage 2 lexes, parses and lowers the integer Core. Sources outside that Core are refused with a diagnostic. |
| `stage2-typed-sidecar` | checkpoint | tooling | Stage 2 projects compiler-derived semantic events one-way into typed-sidecar v1, for explicit single-file `kofun check` only. The sidecar is non-authoritative and compiler, KIF and cache consumers remain forbidden. |
| `stdio-language-server` | checkpoint | tooling | A stdio LSP server providing diagnostics, definitions, hover, completion, document symbols, references, highlights, inlay hints carrying the read/edit/take mode at call sites, signature help, folding, selection ranges, semantic tokens, and rename restricted to locals and parameters, within measured latency budgets. Renaming a function or type is refused because the server reads only open documents; no code actions are offered because no registered diagnostic carries a remedy. |
| `syscall-file-round-trip` | implemented | frameworks | A native ELF program opens, writes, reads back and closes a file through direct syscalls, and reports errno failures explicitly. |
| `syscall-stdlib-api` | checkpoint | frameworks | The syscall and standard-library surface is a Kofun source contract for Linux x86-64. It is not a portable standard library. |
| `test-skip-reporting` | implemented | tooling | `kofun test` reports skipped cases explicitly and counts them separately from passes. |
| `wasm32-arithmetic-core` | checkpoint | webassembly | Checked Int64 arithmetic lowers directly to wasm32 and runs in a lazily initialised browser host. Text and List values are outside the slice. |
| `wasm32-hostabi1-object-arena` | checkpoint | webassembly | The wasm32-hostabi1 target emits one fixed-page linear memory, the v1 export surface, a checked transactional bump allocator, and the recomputed u64 object header encoder for an empty entry point. Text and List lowering remain outside the slice. |
