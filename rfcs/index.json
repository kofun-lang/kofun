{
  "schema": "kofun.rfc-ledger/v1",
  "ledger_version": 1,
  "review_period_days": 14,
  "rfcs": [
    {
      "id": "DD-010",
      "title": "`/` and `//`",
      "provenance": "migrated",
      "state": "implemented",
      "shepherd": "hjosugi",
      "source": "docs/DESIGN_DECISIONS.md",
      "proposal": "https://github.com/kofun-lang/kofun/issues/687",
      "dates": {
        "recorded_on": "2026-07-26"
      },
      "normative_spec": [
        "docs/SYNTAX.md",
        "docs/TYPE_SYSTEM.md",
        "spec/semantics.md"
      ],
      "compatibility": {
        "category": "breaking",
        "statement": "`/` applied to two `Int` operands is a compile error. A program written against the original decision, which expected `7 / 2` to yield a fractional value, no longer compiles.",
        "corpus_query": "git grep -nE '[0-9A-Za-z_)] / [0-9A-Za-z_(]' -- '*.kofun' | grep -v '#' | grep -v '\"'",
        "result": "4 sites, and all four are the `reject_slash_operator.kofun` fixtures that exist to be refused: one each for the native, self-host C11, wasm32, and numeric-conformance paths. No other tracked Kofun source applies `/` to `Int` operands.",
        "evidence": "tests/conformance/numeric/reject_slash_operator.kofun"
      },
      "implementation": {
        "change": "https://github.com/kofun-lang/kofun/issues/687",
        "enablement": "Enabled unconditionally, with no flag. The frontend refuses `/` on `Int` before lowering, and the native x86-64, native AArch64, wasm32 and self-host C11 paths each carry their own refusal fixture so no backend can quietly give the operator a meaning.",
        "gate": "sh tests/conformance/run.sh",
        "claims": [
          "checked-int64-contract",
          "native-integer-division"
        ]
      },
      "amendments": [
        {
          "id": "A01",
          "recorded_on": "2026-07-26",
          "change": "https://github.com/kofun-lang/kofun/issues/687",
          "delta": "`/` is no longer an `Int` operator at all. It is reserved and refused with one diagnostic rather than returning a fractional value, because no fractional type exists to return. `//` is unchanged and remains floor division.",
          "original_semantics": "`/` returns floating division. `//` performs integer/floor division.",
          "compatibility": {
            "category": "breaking",
            "statement": "Three documents said `/` on `Int` yielded `Float` while four backends truncated and one refused the operator outright. The amendment makes refusal the single stated meaning, so the documents and the backends agree.",
            "corpus_query": "./bin/kofun test tests/conformance/numeric",
            "result": "12 numeric conformance cases execute on every declared backend. The only case applying `/` is `reject_slash_operator.kofun`, and every declared backend refuses it identically.",
            "evidence": "tests/conformance/numeric/reject_slash_operator.kofun"
          }
        }
      ]
    },
    {
      "id": "DD-012",
      "title": "No silent backend fallback",
      "provenance": "migrated",
      "state": "implemented",
      "shepherd": "hjosugi",
      "source": "docs/DESIGN_DECISIONS.md",
      "proposal": "https://github.com/kofun-lang/kofun/issues/666",
      "dates": {
        "recorded_on": "2026-07-26"
      },
      "normative_spec": [
        "README.md",
        "docs/NATIVE_BACKEND.md"
      ],
      "compatibility": {
        "category": "none",
        "statement": "No accepted program changes meaning. The decision constrains implementations: a backend that cannot lower a construct must fail with a source-located error or be recorded as unsupported, and may not substitute a different construct.",
        "evidence": "tests/conformance/capabilities.tsv"
      },
      "implementation": {
        "change": "https://github.com/kofun-lang/kofun/issues/666",
        "enablement": "Enabled for every registered backend/corpus pair. `tests/conformance/capabilities.tsv` is the closed manifest of supported and unsupported pairs; adapters no longer carry an independently authoritative corpus list, so a backend cannot declare its own coverage.",
        "gate": "sh tests/conformance/check-capabilities.sh",
        "claims": [
          "checked-int64-contract",
          "native-integer-division",
          "wasm32-arithmetic-core"
        ]
      }
    },
    {
      "id": "DD-013",
      "title": "Typed hygienic macros",
      "provenance": "migrated",
      "state": "accepted",
      "shepherd": "hjosugi",
      "source": "docs/DESIGN_DECISIONS.md",
      "dates": {
        "recorded_on": "2026-07-26"
      },
      "normative_spec": [
        "docs/METAPROGRAMMING.md"
      ],
      "compatibility": {
        "category": "none",
        "statement": "Nothing is implemented, so no program changes meaning. The decision binds a future implementation: quote and unquote operate on token trees or a typed public AST, and there is no C-style textual preprocessor."
      }
    },
    {
      "id": "DD-016",
      "title": "Algebraic laws are compiler artifacts",
      "provenance": "migrated",
      "state": "accepted",
      "shepherd": "hjosugi",
      "source": "docs/DESIGN_DECISIONS.md",
      "proposal": "https://github.com/kofun-lang/kofun/issues/551",
      "dates": {
        "recorded_on": "2026-07-26"
      },
      "normative_spec": [
        "docs/LAW_SYSTEM.md",
        "docs/SYNTAX.md",
        "docs/TYPE_SYSTEM.md",
        "spec/grammar.ebnf",
        "spec/semantics.md"
      ],
      "compatibility": {
        "category": "none",
        "statement": "Nothing is implemented, so no accepted program changes meaning. The decision binds a future implementation: `law` is contextual, family names are ordinary identifiers, and a family, implementation, and check request remain separate source declarations."
      }
    },
    {
      "id": "DD-017",
      "title": "Evidence levels are never conflated",
      "provenance": "migrated",
      "state": "accepted",
      "shepherd": "hjosugi",
      "source": "docs/DESIGN_DECISIONS.md",
      "proposal": "https://github.com/kofun-lang/kofun/issues/551",
      "dates": {
        "recorded_on": "2026-07-26"
      },
      "normative_spec": [
        "docs/LAW_SYSTEM.md",
        "spec/semantics.md"
      ],
      "compatibility": {
        "category": "none",
        "statement": "Nothing emits executable law evidence, so no accepted program or artifact changes meaning. The decision constrains future checkers to compute and preserve distinct `bounded-exhaustive`, `proven-finite`, and `proven` assurance levels under deterministic finite evaluation."
      }
    },
    {
      "id": "DD-018",
      "title": "Versioned machine-readable law evidence",
      "provenance": "migrated",
      "state": "accepted",
      "shepherd": "hjosugi",
      "source": "docs/DESIGN_DECISIONS.md",
      "dates": {
        "recorded_on": "2026-07-26"
      },
      "normative_spec": [
        "spec/law-evidence-v2.schema.json"
      ],
      "compatibility": {
        "category": "breaking",
        "statement": "`kofun.law-evidence/v2` is deliberately incompatible with v1. A v1 document is historical migration material and is never interpreted as v2, so no v1 evidence can authorize a v2 optimization or cache hit. Nothing emits either version yet, so nothing in the repository is affected today.",
        "evidence": "spec/law-evidence.schema.json"
      }
    },
    {
      "id": "DD-021",
      "title": "Records declare with `type` and construct with labels",
      "provenance": "migrated",
      "state": "implemented",
      "shepherd": "hjosugi",
      "source": "docs/DESIGN_DECISIONS.md",
      "proposal": "https://github.com/kofun-lang/kofun/issues/546",
      "dates": {
        "recorded_on": "2026-07-27"
      },
      "normative_spec": [
        "docs/SYNTAX.md",
        "docs/TYPE_SYSTEM.md",
        "spec/records-v1.md"
      ],
      "compatibility": {
        "category": "additive",
        "statement": "The bounded record frontend adds nominal declarations and labelled construction without changing the meaning of an existing accepted program. The main compiler and backends do not enable this surface yet.",
        "evidence": "tests/conformance/records/README.md"
      },
      "implementation": {
        "change": "https://github.com/kofun-lang/kofun/pull/756",
        "enablement": "Enabled only in the bounded record frontend invoked by `task records`. Module visibility, generic records, the main compiler, and backend lowering remain outside the checkpoint.",
        "gate": "task records",
        "claims": [
          "nominal-records"
        ]
      }
    },
    {
      "id": "DD-024",
      "title": "Four semantic namespaces",
      "provenance": "migrated",
      "state": "accepted",
      "shepherd": "hjosugi",
      "source": "docs/DESIGN_DECISIONS.md",
      "proposal": "https://github.com/kofun-lang/kofun/issues/290",
      "dates": {
        "recorded_on": "2026-07-22"
      },
      "normative_spec": [
        "spec/modules/namespaces.md"
      ],
      "compatibility": {
        "category": "additive",
        "statement": "Assigning every declaration to one of four syntax-directed namespaces adds an identity domain rather than a restriction. Capitalization stays non-semantic, no source form that resolves today is refused by this decision, and the namespace tag becomes a new input to identity and serialization."
      }
    },
    {
      "id": "DD-025",
      "title": "An explicit `module` header is the only module authority",
      "provenance": "migrated",
      "state": "accepted",
      "shepherd": "hjosugi",
      "source": "docs/DESIGN_DECISIONS.md",
      "proposal": "https://github.com/kofun-lang/kofun/issues/300",
      "dates": {
        "recorded_on": "2026-07-22"
      },
      "normative_spec": [
        "spec/modules/source-file-mapping.md"
      ],
      "compatibility": {
        "category": "conditional",
        "statement": "Breaking for two shapes: a manifest source with no `module` header, and an anonymous single-file operand that carries one. Both are refused rather than given a fallback identity, because a fallback would make a file move a semantic rename.",
        "corpus_query": "git grep -lE '^module ' -- '*.kofun' | wc -l; git ls-files '*.kofun' | wc -l",
        "result": "96 of 677 tracked Kofun sources declare a header. All 96 sit under `tests/conformance/` module, ADT-exhaustiveness, and incremental fixtures; no tracked example, benchmark, stdlib, or bootstrap source carries one."
      }
    },
    {
      "id": "DD-026",
      "title": "One digest never does two jobs",
      "provenance": "migrated",
      "state": "accepted",
      "shepherd": "hjosugi",
      "source": "docs/DESIGN_DECISIONS.md",
      "proposal": "https://github.com/kofun-lang/kofun/issues/303",
      "dates": {
        "recorded_on": "2026-07-22"
      },
      "normative_spec": [
        "spec/modules/module-identity.md"
      ],
      "compatibility": {
        "category": "additive",
        "statement": "KIF, the framed SHA-256 preimage, and the three separate semantic and ABI digests are a new compiler-authoritative interface format. No source program changes meaning, and no existing artifact is reinterpreted: a v1 identity exists only where the versioned envelope was written."
      }
    },
    {
      "id": "DD-027",
      "title": "Re-exports are explicit and never widen",
      "provenance": "migrated",
      "state": "implemented",
      "shepherd": "hjosugi",
      "source": "docs/DESIGN_DECISIONS.md",
      "proposal": "https://github.com/kofun-lang/kofun/issues/287",
      "dates": {
        "recorded_on": "2026-07-23"
      },
      "normative_spec": [
        "spec/modules/re-exports.md"
      ],
      "compatibility": {
        "category": "additive",
        "statement": "`pub` on an import is new surface. An ordinary `import` keeps its meaning as a private binding, and no declaration becomes publicly reachable without the modifier.",
        "corpus_query": "git grep -lE '^pub (import|from) ' -- '*.kofun'",
        "result": "8 tracked sources use the form: 5 re-export fixtures, 1 import-alias fixture, and 2 incremental fixtures. Every one of them was written for this decision's gates.",
        "evidence": "tests/conformance/modules/re-exports/run.sh"
      },
      "implementation": {
        "change": "https://github.com/kofun-lang/kofun/issues/602",
        "enablement": "Same-package `pub import` and `pub from` in the Stage 2 frontend, with non-widening `ExportBindingId` edges and the stated bounds: 64-edge chains, 1,024 bindings per module, 65,536 edges per package. There is no `internal` re-export form, and cross-package re-export is outside the checkpoint.",
        "gate": "task re-exports",
        "claims": [
          "public-re-exports"
        ]
      }
    },
    {
      "id": "DD-028",
      "title": "The typed sidecar is never authoritative",
      "provenance": "migrated",
      "state": "implemented",
      "shepherd": "hjosugi",
      "source": "docs/DESIGN_DECISIONS.md",
      "proposal": "https://github.com/kofun-lang/kofun/issues/601",
      "dates": {
        "recorded_on": "2026-07-23"
      },
      "normative_spec": [
        "spec/tooling/typed-sidecar.md",
        "spec/typed-sidecar/kofun.typed-sidecar.v1.schema.json"
      ],
      "compatibility": {
        "category": "additive",
        "statement": "The sidecar is a new artifact that no compiler path reads. It is emitted on request, declares `\"authoritative\": false`, and cannot become an input, so no program or build changes meaning.",
        "evidence": "tests/typed-sidecar/authority-boundary.sh"
      },
      "implementation": {
        "change": "https://github.com/kofun-lang/kofun/pull/714",
        "enablement": "Stage 2 projects compiler-derived semantic events one-way into typed-sidecar v1 for an explicit single-file `kofun check`. The document is non-authoritative, and compiler, KIF, and cache consumers are refused by the authority-boundary gate.",
        "gate": "task typed-sidecar-projector",
        "claims": [
          "stage2-typed-sidecar"
        ]
      }
    },
    {
      "id": "DD-029",
      "title": "Discovery is compiler-backed",
      "provenance": "migrated",
      "state": "accepted",
      "shepherd": "hjosugi",
      "source": "docs/DESIGN_DECISIONS.md",
      "proposal": "https://github.com/kofun-lang/kofun/issues/635",
      "dates": {
        "recorded_on": "2026-07-25"
      },
      "normative_spec": [
        "docs/DEVELOPER_DISCOVERY.md"
      ],
      "compatibility": {
        "category": "additive",
        "statement": "The `kofun.discovery.request/v1` and `kofun.discovery.result/v1` records are a new tooling surface. Results are non-authoritative and forbidden as compiler, KIF, build, or release input, so nothing that compiles today changes meaning."
      }
    },
    {
      "id": "DD-030",
      "title": "Unsuffixed fractional literals are `Decimal`",
      "provenance": "migrated",
      "state": "accepted",
      "shepherd": "hjosugi",
      "source": "docs/DESIGN_DECISIONS.md",
      "proposal": "https://github.com/kofun-lang/kofun/issues/545",
      "dates": {
        "recorded_on": "2026-07-26"
      },
      "normative_spec": [
        "docs/DECIMAL.md"
      ],
      "compatibility": {
        "category": "conditional",
        "statement": "Breaking for a program that relies on an unsuffixed fractional literal denoting `Float`: under this decision it denotes `Decimal`, and a binary64 value requires the explicit suffix. Mixed `Int`, `Decimal`, and `Float` arithmetic is refused rather than promoted.",
        "corpus_query": "git grep -nE '(^|[^0-9A-Za-z_.\"])[0-9]+\\.[0-9]+([^0-9A-Za-z_.\"]|$)' -- '*.kofun' | grep -vE '^[^:]+:[0-9]+:\\s*#' | grep -vE '\"[^\"]*[0-9]+\\.[0-9]+[^\"]*\"'",
        "result": "20 sites in 11 tracked sources. 8 are fixtures that exist to assert this rule -- `d001`, `e2s99`, `e2s100`, `e2s101`, `e2s103`, the two `reject_` numeric conformance fixtures, and the Stage 2 decimal token fixture. The other 12 sites, in `tests/kofun/scientific.kofun`, `examples/science.kofun` and `benchmarks/vector.kofun`, pass unsuffixed literals into Float-shaped scientific calls and need the suffix."
      }
    },
    {
      "id": "DD-031",
      "title": "Type-level programming is named and terminating",
      "provenance": "migrated",
      "state": "accepted",
      "shepherd": "hjosugi",
      "source": "docs/DESIGN_DECISIONS.md",
      "proposal": "https://github.com/kofun-lang/kofun/issues/657",
      "dates": {
        "recorded_on": "2026-07-26"
      },
      "normative_spec": [
        "spec/type-level-programming-v1.md"
      ],
      "compatibility": {
        "category": "additive",
        "statement": "The `kofun.type-reduction/default-v1` profile adds a module-level declaration form. Anonymous type expressions and general recursive type programs are refused by the profile rather than removed from the language, because neither form exists today.",
        "corpus_query": "git grep -nE '^type fn ' -- '*.kofun' | wc -l",
        "result": "0 sites. No tracked Kofun source declares a type-level function."
      },
      "amendments": [
        {
          "id": "A01",
          "recorded_on": "2026-08-09",
          "change": "https://github.com/kofun-lang/kofun/issues/1130",
          "delta": "General recursive and mutually recursive type programs are admissible when declared `type fn general`, and Turing-complete type programming is no longer rejected as a goal. A general declaration reduces under the new profile `kofun.type-reduction/general-v2`, whose frame, step and node limits are versioned language semantics; exhausting one is a deterministic type error naming the declaration, the limit, the measured counts and the top consumers, rather than a hang. Structural termination becomes a checked property of the unmarked `type fn` instead of the only admissible form, and a structural declaration may not call a general one, so a `kofun.type-reduction/default-v1` root keeps the v1 guarantee compositionally. RFC-0009 separately adds the `Nat`, `Symbol` and `Bool` data kinds and the `kofun.type-reduction/kinds-v1` profile, which the Type-only wording of this decision did not admit. The rejection of anonymous conditional, mapped and inferred type expressions is not amended and stands as written.",
          "original_semantics": "Type-level logic is a module-level declaration with a stable identity and never an anonymous type expression at a use site. The v1 profile `kofun.type-reduction/default-v1` is Type-only, named, and structurally terminating. Anonymous conditional, mapped, or inferred type expressions are rejected because diagnostics would depend on anonymous internals. General recursive and mutually recursive type programs are rejected because termination, cost, and error size are not statically bounded, and Turing-complete type programming is rejected as a goal. Higher-kinded and effectful type computation is deferred, not refused.",
          "compatibility": {
            "category": "additive",
            "statement": "No program changes meaning and none stops compiling. The amendment widens what the profile admits and removes no accepted form: the unmarked `type fn` keeps every v1 rule, its limits, and its trace byte for byte, and the new surface -- the `general` modifier, the `general-v2` and `kinds-v1` profiles, the three data kinds, the eleven builtins, the two views, the `kofun.type-reduction-trace/v2` schema, and codes E390 through E401 -- is reachable only from declarations that do not exist. No active compiler parses `type fn` at all, so no tracked source can observe the widened profile, and the deferral of higher-kinded and effectful type computation is narrowed only where RFC-0009 decides the kind axis; effectful type computation stays deferred.",
            "corpus_query": "git grep -nE '^type fn ' -- '*.kofun' | wc -l; git grep -nE '^type fn general ' -- '*.kofun' | wc -l; git grep -nE '\\[[A-Za-z_]+:[[:space:]]*(Nat|Symbol|Bool)\\b' -- '*.kofun' | wc -l",
            "result": "`0`, `0` and `0` at 2cfa9bdd, the audited commit. No tracked Kofun source declares a type-level function, declares a general one, or annotates a type parameter with a data kind, so the count of tracked programs whose meaning the amendment changes is 0.",
            "evidence": "spec/type-level-programming-v1.md"
          }
        }
      ]
    },
    {
      "id": "DD-032",
      "title": "`trait`, with a local-trait-or-local-outer-type orphan rule",
      "provenance": "migrated",
      "state": "accepted",
      "shepherd": "hjosugi",
      "source": "docs/DESIGN_DECISIONS.md",
      "proposal": "https://github.com/kofun-lang/kofun/issues/403",
      "dates": {
        "recorded_on": "2026-07-26"
      },
      "normative_spec": [
        "spec/roadmap-31-34/generics-and-traits.md"
      ],
      "compatibility": {
        "category": "additive",
        "statement": "The orphan rule and the dictionary-passing baseline constrain implementations written after them; the specification marks the lowering baseline as provisional until its experiment is recorded, and the coherence rule as accepted. The bounded frontend that enforces the rule is gated by `task traits`, and the state stays `accepted` because release/claims.json records no capability claim for it.",
        "corpus_query": "git grep -lE '^(trait|impl) ' -- '*.kofun' | wc -l",
        "result": "18 files, all under `tests/conformance/traits` and written for the bounded #332 frontend, which refuses a foreign trait implemented for a foreign type with `E2S131`. No other tracked source declares a trait or an implementation.",
        "evidence": "tests/conformance/traits/orphan_both_foreign.stderr"
      }
    },
    {
      "id": "DD-033",
      "title": "Layout is target-parameterized, not target-identical",
      "provenance": "migrated",
      "state": "accepted",
      "shepherd": "hjosugi",
      "source": "docs/DESIGN_DECISIONS.md",
      "proposal": "https://github.com/kofun-lang/kofun/issues/120",
      "dates": {
        "recorded_on": "2026-07-27"
      },
      "normative_spec": [
        "spec/aggregate-layout-v1.md"
      ],
      "compatibility": {
        "category": "additive",
        "statement": "The contract defines byte layout only and does not change code generation. A backend that does not claim AggregateLayout v1 is unaffected; one that does must agree with the golden vectors, and its own check asserts that a 32-bit and a 64-bit target do not produce identical descriptors.",
        "evidence": "spec/aggregate-layout-v1/check.sh"
      },
      "amendments": [
        {
          "id": "A01",
          "recorded_on": "2026-08-10",
          "change": "https://github.com/kofun-lang/kofun/issues/1183",
          "delta": "AggregateLayout v1 admits a sixth value kind, `bounded_list`: a fixed-capacity list stored inline and by value, with a `u64` length at offset 0, a fixed element array beginning at `align_up(8, element_align)`, alignment `max(header_align, element_align)` rather than size, the element pointer bitmap repeated per slot, and trivial drop when that bitmap is empty. Capacity is part of type identity. The `list` kind is not amended: a `List` value is still exactly one reference, with unchanged size, alignment, pointer bitmap and managed drop, and every committed golden vector stays byte-identical. What moves is that `List` is no longer the only way a list-shaped value may be laid out, so the sentence \"a Text or List value is exactly one reference\" now describes kind `list` rather than every list-shaped field. RFC-0011 separately decides that the Stage 2 C11 bootstrap profile lowers a `List[Int]` record field to `bounded_list[Int, 64]`; that mapping is a property of the profile, not of `List[Int]` in the language, and no other element type is admitted. The target-parameterization decision itself is not amended and stands as written: a `bounded_list` descriptor is still computed from the declared target, and a 32-bit and a 64-bit target still disagree.",
          "original_semantics": "`Text`, `List`, flat records, and flat ADT variants get deterministic byte layouts computed from a declared `TargetDataLayout`. Layout identity is the v1 schema plus the complete target parameters plus type identity, so one type on two targets is two descriptors and neither is privileged: a 32-bit target gets 4-byte references and a 64-bit target 8-byte references without either one changing source semantics. One byte-identical layout on every target is rejected — it decides wasm32 pointer width in advance and hides that decision inside a diff that looks like formatting. An opaque handle for every aggregate is rejected as the default representation, because it costs an allocation and a dereference per aggregate and forfeits the predictable native layout this contract exists to give.",
          "compatibility": {
            "category": "additive",
            "statement": "No tracked program changes meaning and none stops compiling. `bounded_list` is a new kind name reachable from no existing layout vector, and the `list`, `text`, `scalar`, `record`, `adt` and `optional` kinds are unchanged, so the committed golden vectors and the target-divergence assertion in `spec/aggregate-layout-v1/check.sh` are unaffected. The change is additive in the direction that matters: record fields that the Stage 2 backend refuses today become lowerable, and nothing that lowers today is refused.",
            "corpus_query": "git grep -nE '^[[:space:]]*[a-z_][A-Za-z0-9_]*:[[:space:]]*List\\[Int\\]' -- '*.kofun' | wc -l; git grep -c 'outside the Stage 2 Int/Bool/Text slice' -- 'stdlib/**/verify.sh' | wc -l",
            "result": "`19` and `11` at 43d53d38, the audited commit. Nineteen record fields across six tracked files are typed `List[Int]`, and eleven standard-library verify scripts assert the current refusal verbatim; four of those eleven — `stdlib/array`, `stdlib/binary_heap`, `stdlib/set` and `stdlib/vector` — guard records whose refused field is one of the nineteen, so the count of tracked sources whose Stage 2 result changes is 4.",
            "evidence": "spec/aggregate-layout-v1/layout.mjs"
          }
        }
      ]
    },
    {
      "id": "DD-034",
      "title": "Validation accumulates in `Validated`, not `Result`",
      "provenance": "migrated",
      "state": "accepted",
      "shepherd": "hjosugi",
      "source": "docs/DESIGN_DECISIONS.md",
      "proposal": "https://github.com/kofun-lang/kofun/issues/742",
      "dates": {
        "recorded_on": "2026-07-28"
      },
      "normative_spec": [
        "spec/effects/validation-accumulation.md"
      ],
      "compatibility": {
        "category": "additive",
        "statement": "`Validated`, its combinators, and the opaque `Issues[E]` are new surface beside `Result`, which keeps its meaning. The five questions the design left open were decided as recommended; a later change to the states, operations, ordering, effect rule, ownership boundary, or complexity bound is an amendment to this decision.",
        "corpus_query": "git grep -lE '\\bValidated\\b' -- '*.kofun' | wc -l",
        "result": "0 files. No tracked Kofun source mentions `Validated`, and no library, compiler path, or backend implements the contract."
      }
    },
    {
      "id": "DD-035",
      "title": "Laws are library declarations checked by a finite-model engine",
      "provenance": "migrated",
      "state": "accepted",
      "shepherd": "hjosugi",
      "source": "docs/DESIGN_DECISIONS.md",
      "proposal": "https://github.com/kofun-lang/kofun/issues/551",
      "dates": {
        "recorded_on": "2026-07-31"
      },
      "normative_spec": [
        "docs/LAW_SYSTEM.md",
        "spec/law-evidence-v2.schema.json"
      ],
      "compatibility": {
        "category": "additive",
        "statement": "`law`, `instance`, and `model` are new contextual top-level declarations; no family name is a keyword. The active compiler rejects every existing `law` example today, so no program changes meaning when the declarations start to parse.",
        "corpus_query": "git grep -l '^law ' -- '*.kofun' | wc -l",
        "result": "3 files, all historical examples that `kofun check` currently rejects with E2S02; none participates in a build."
      }
    },
    {
      "id": "DD-036",
      "title": "Result propagation is postfix `?`, monomorphic to `Result`",
      "provenance": "migrated",
      "state": "accepted",
      "shepherd": "hjosugi",
      "source": "docs/DESIGN_DECISIONS.md",
      "proposal": "https://github.com/kofun-lang/kofun/issues/626",
      "dates": {
        "recorded_on": "2026-07-31"
      },
      "normative_spec": [
        "spec/result-propagation-v1.md"
      ],
      "compatibility": {
        "category": "additive",
        "statement": "Postfix `?` on expressions is currently a parse error, so giving it Result-propagation meaning breaks no existing program. `T?` in type position keeps its DD-002 meaning untouched.",
        "corpus_query": "git grep -nE '[[:alnum:]_)]\\?[[:space:];)]' -- '*.kofun' | wc -l",
        "result": "17 sites, and every one is a `T?` optional type suffix in type position; no tracked source contains an expression-position postfix `?`."
      }
    },
    {
      "id": "DD-037",
      "title": "Streams are a library protocol with explicit demand",
      "provenance": "migrated",
      "state": "accepted",
      "shepherd": "hjosugi",
      "source": "docs/DESIGN_DECISIONS.md",
      "proposal": "https://github.com/kofun-lang/kofun/issues/627",
      "dates": {
        "recorded_on": "2026-07-31"
      },
      "normative_spec": [
        "docs/stdlib/stream-protocol.md"
      ],
      "compatibility": {
        "category": "additive",
        "statement": "`Stream[T, E]`, `Subscription`, and the demand/overflow contract are new library surface; no language keyword is added and `Channel` remains a distinct abstraction behind an explicit adapter.",
        "corpus_query": "git grep -lE '\\bStream\\b' -- '*.kofun' | wc -l",
        "result": "0 files. No tracked Kofun source mentions `Stream`, and no library, compiler path, or backend implements the protocol."
      }
    },
    {
      "id": "DD-038",
      "title": "The standard library ships in four tiers",
      "provenance": "migrated",
      "state": "accepted",
      "shepherd": "hjosugi",
      "source": "docs/DESIGN_DECISIONS.md",
      "proposal": "https://github.com/kofun-lang/kofun/issues/636",
      "dates": {
        "recorded_on": "2026-07-31"
      },
      "normative_spec": [
        "docs/STANDARD_LIBRARY_CHARTER.md"
      ],
      "compatibility": {
        "category": "none",
        "statement": "The charter assigns tiers, states, and update policy to library capabilities; it changes no program semantics. The machine-checked matrix keeps every coverage claim bounded by evidence."
      }
    },
    {
      "id": "DD-039",
      "title": "The HTTP client is bounded, capability-explicit, HTTP/1.1 first",
      "provenance": "migrated",
      "state": "accepted",
      "shepherd": "hjosugi",
      "source": "docs/DESIGN_DECISIONS.md",
      "proposal": "https://github.com/kofun-lang/kofun/issues/638",
      "dates": {
        "recorded_on": "2026-07-31"
      },
      "normative_spec": [
        "docs/stdlib/http-client.md"
      ],
      "compatibility": {
        "category": "additive",
        "statement": "The client is a new T3 module; the closed #24 HTTP server framework keeps its documented bounded profile and is not evidence for this contract. No client is implemented.",
        "corpus_query": "git grep -lE '\\bhttp\\.' -- '*.kofun' | wc -l",
        "result": "0 files. No tracked Kofun source calls the proposed client API."
      }
    },
    {
      "id": "DD-040",
      "title": "Time is six types; zones are versioned data; clocks are capabilities",
      "provenance": "migrated",
      "state": "accepted",
      "shepherd": "hjosugi",
      "source": "docs/DESIGN_DECISIONS.md",
      "proposal": "https://github.com/kofun-lang/kofun/issues/639",
      "dates": {
        "recorded_on": "2026-07-31"
      },
      "normative_spec": [
        "docs/stdlib/date-time.md"
      ],
      "compatibility": {
        "category": "additive",
        "statement": "Civil types, `Resolve` disambiguation, RFC 3339 profiles, and versioned tz data are new surface. The stdlib/clock seed keeps its syscall-level per-clock `Instant`; the portable contract absorbs that boundary rather than changing it.",
        "corpus_query": "git grep -lE '\\bInstant\\b|\\bZoned\\b' -- '*.kofun' | wc -l",
        "result": "2 files, both in the stdlib/clock syscall seed; no civil calendar, zone, or RFC 3339 code exists."
      }
    },
    {
      "id": "DD-041",
      "title": "Benchmarks are declared, raw-sample-first, and honest about counters",
      "provenance": "migrated",
      "state": "accepted",
      "shepherd": "hjosugi",
      "source": "docs/DESIGN_DECISIONS.md",
      "proposal": "https://github.com/kofun-lang/kofun/issues/640",
      "dates": {
        "recorded_on": "2026-07-31"
      },
      "normative_spec": [
        "docs/stdlib/benchmark.md"
      ],
      "compatibility": {
        "category": "additive",
        "statement": "`bench` declarations, the `Bench` API, and the kofun.bench-report/v1 schema are new surface; repository benchmark scripts remain internal evidence and no public harness exists.",
        "corpus_query": "git grep -lE '^bench ' -- '*.kofun' | wc -l",
        "result": "0 files. No tracked Kofun source declares a benchmark."
      }
    },
    {
      "id": "RFC-0001",
      "title": "Allocator choice is a scoped, effect-tracked capability",
      "provenance": "rfc",
      "state": "accepted",
      "shepherd": "hjosugi",
      "source": "rfcs/0001-allocator-capability.md",
      "document": "rfcs/0001-allocator-capability.md",
      "proposal": "https://github.com/kofun-lang/kofun/issues/573",
      "dates": {
        "opened_on": "2026-08-01",
        "review_closed_on": "2026-08-09",
        "decided_on": "2026-08-09"
      },
      "normative_spec": [
        "rfcs/0001-allocator-capability.md"
      ],
      "compatibility": {
        "category": "additive",
        "statement": "Everything proposed is new surface: the `alloc` effect label, the `Alloc` capability and its authority derivation, region-tagged values, diagnostics E340-E343 and R030, capability-taking standard-library variants, and a contextual `with` allocator scope. The managed default stays the default and existing signatures keep their meaning, so no tracked program changes meaning or stops compiling.",
        "corpus_query": "git grep -niE '\\ballocator\\b' -- '*.kofun' | wc -l; git grep -nE '^[[:space:]]*with\\b' -- '*.kofun' | wc -l",
        "result": "0 and 0 across 761 tracked `.kofun` sources: no tracked Kofun source names an allocator, and none begins a statement with `with`, so the contextual keyword and the new surface collide with nothing."
      }
    },
    {
      "id": "RFC-0002",
      "title": "Environment access requires attenuated affine authority",
      "provenance": "rfc",
      "state": "accepted",
      "shepherd": "hjosugi",
      "source": "rfcs/0002-environment-authority.md",
      "document": "rfcs/0002-environment-authority.md",
      "proposal": "https://github.com/kofun-lang/kofun/issues/569",
      "dates": {
        "opened_on": "2026-08-02",
        "review_closed_on": "2026-08-09",
        "decided_on": "2026-08-09"
      },
      "normative_spec": [
        "rfcs/0002-environment-authority.md"
      ],
      "compatibility": {
        "category": "additive",
        "statement": "Everything proposed is new target surface: runtime-created `RootAuthority`, key-bounded affine `EnvironmentAuthority`, portable `EnvironmentKey`, explicit derivation and attenuation, diagnostics E350-E356, and audit-only package metadata. No existing safe environment API changes, RFC-0001 is unchanged, and no tracked Kofun source collides with these names.",
        "corpus_query": "git grep -nE 'RootAuthority|Environment(Capability|Authority)|Environment\\.(acquire|get)' -- '*.kofun' 'spec/**' 'stdlib/**'",
        "result": "0 matches across 763 tracked `.kofun` sources at e270143665789e452495ea1bdf13e17db9bbf779; grep exits 1 with no output, so no tracked source or named spec/stdlib path contains the pre-RFC authority surface."
      }
    },
    {
      "id": "RFC-0003",
      "title": "Scoped parallelism uses lexical spawn/join ownership",
      "provenance": "rfc",
      "state": "accepted",
      "shepherd": "hjosugi",
      "source": "rfcs/0003-scoped-parallelism.md",
      "document": "rfcs/0003-scoped-parallelism.md",
      "proposal": "https://github.com/kofun-lang/kofun/issues/555",
      "dates": {
        "opened_on": "2026-08-02",
        "review_closed_on": "2026-08-09",
        "decided_on": "2026-08-09"
      },
      "normative_spec": [
        "rfcs/0003-scoped-parallelism.md",
        "spec/concurrency/scoped-parallelism-v1.md"
      ],
      "compatibility": {
        "category": "additive",
        "statement": "The lexical `par` scope, direct scope-token `spawn`, consuming `join`, second-class task handles, ownership conflict classes, and bounded model are new target semantics. No released parser accepts the forms, no existing accepted program changes meaning, and no tracked Kofun source uses the proposed surface.",
        "corpus_query": "git grep -nE '\\bpar[[:space:]]*\\||\\.spawn\\(|\\.join\\(' -- '*.kofun'",
        "result": "0 matches across 799 tracked `.kofun` sources at 516e8fb3925088dd64b291e8cd749764a41891b1; grep exits 1 with no output, so no tracked source uses the proposed `par`, `.spawn(`, or `.join(` surface."
      }
    },
    {
      "id": "RFC-0004",
      "title": "Ownership kind is classified structurally through ADTs, tuples, and generic instantiation",
      "provenance": "rfc",
      "state": "accepted",
      "shepherd": "hjosugi",
      "source": "rfcs/0004-ownership-kind-classification.md",
      "document": "rfcs/0004-ownership-kind-classification.md",
      "proposal": "https://github.com/kofun-lang/kofun/issues/907",
      "dates": {
        "opened_on": "2026-08-02",
        "review_closed_on": "2026-08-09",
        "decided_on": "2026-08-09"
      },
      "normative_spec": [
        "rfcs/0004-ownership-kind-classification.md"
      ],
      "compatibility": {
        "category": "conditional",
        "statement": "The proposal adds refusals to types that already exist, so it is not purely additive. The breaking shape is stated exactly: a program that places an owned value inside a record, tuple, ADT constructor payload, closure capture, `Optional`, `List`, or generic instantiation, and then uses that composite where a managed kind is required (`E360`-`E364`). The two-point Managed/Owned lattice, the structural join, the K-SUBST/K-PARAM generic rules, the `List[T]` owned-element refusal, and the four kind constraints are otherwise new surface. The owned domain does not parse in any compiler slice today, so no tracked program can express the refused shape yet.",
        "corpus_query": "git grep -lE '^[[:space:]]*let[[:space:]]+own[[:space:]]' -- '*.kofun' | wc -l; git grep -nE '\\b(trait|impl)[[:space:]]+(Copy|Managed|Owned)\\b|:[[:space:]]*(Copy|Managed|Owned)\\b' -- '*.kofun' | wc -l; git ls-files -- '*.kofun' | wc -l",
        "result": "5, 0 and 824 at 6f71b6df5cf54aa16542b89494d2194c4172a97e. Five of 824 tracked `.kofun` sources bind an owned value, and all five were read: examples/ownership.kofun, stdlib/tests/file_roundtrip.kofun, tests/conformance/syntax/issues_35_47/structural_surface.kofun, tests/conformance/syntax/issues_35_47/unsupported_owned_binding.kofun, and tests/kofun/ownership.kofun. None places its owned binding in a record, tuple, ADT payload, list, closure capture, or generic instantiation, so the count of tracked programs carrying the refused shape is 0. The second query returns 0: no tracked source declares or bounds on `Copy`, `Managed`, or `Owned`; `Copy` occurs in tracked `.kofun` sources only inside comments and one E007 message string."
      }
    },
    {
      "id": "RFC-0005",
      "title": "A trait or type member scope is closed under direct declaration",
      "provenance": "rfc",
      "state": "accepted",
      "shepherd": "hjosugi",
      "source": "rfcs/0005-trait-member-scope-closure.md",
      "document": "rfcs/0005-trait-member-scope-closure.md",
      "proposal": "https://github.com/kofun-lang/kofun/issues/995",
      "dates": {
        "opened_on": "2026-08-02",
        "review_closed_on": "2026-08-09",
        "decided_on": "2026-08-09"
      },
      "normative_spec": [
        "rfcs/0005-trait-member-scope-closure.md"
      ],
      "compatibility": {
        "category": "additive",
        "statement": "Every shape the rule refuses is already refused, so only the message changes: `E370` replaces a parameter-scope refusal, `E371` replaces a delimiter parse error, and `E372` replaces a slice-boundary message. New surface is the member key (owner identity, namespace, normalized name), the closure rule, the statement that a field collides with an associated value under one type owner but never with a trait method, and the three codes. No accepted program changes meaning and none stops compiling: a member scope with no inherited and no default member has no collision an accepted program could already contain.",
        "corpus_query": "git ls-files -- '*.kofun' | wc -l; git grep -nwE 'extends|override' -- '*.kofun' | wc -l; git grep -nE '^(trait|foreign trait|impl)|^    fn ' -- '*.kofun' | awk -F: '{ sub(/^[^:]*:[^:]*:/, \"\") } $0 ~ /^(trait|foreign trait|impl)/ { if (n > 1) m++; n = 0; b++; next } { n++ } END { if (n > 1) m++; print b, m + 0 }'",
        "result": "`888`, `0`, and `42 0` at 727f9dac442ba8db56e8344dbd1e29112ea1e64d, the audited commit. Of 888 tracked `.kofun` sources, 0 name an inheritance edge or an override marker, and of the 42 trait and implementation member scopes they declare, 0 declare more than one member. So the count of tracked programs this rule refuses that are accepted today is 0, and the count that would need an override marker is 0. The same query returns `890`, `1`, and `45 1` after this change, and both differences are the two negative fixtures it adds: tests/conformance/traits/inherited_member_source.kofun is the one source spelling `extends`, and tests/conformance/traits/member_name_collision.kofun is the one owner declaring two members. Both are refused by the frontend today and pinned by byte-exact goldens, so the count of *accepted* programs in either shape stays 0.",
        "evidence": "tests/conformance/traits/member_name_collision.stderr"
      }
    },
    {
      "id": "RFC-0006",
      "title": "A module-level binding is language surface, and v1 is the immutable integer constant",
      "provenance": "rfc",
      "state": "accepted",
      "shepherd": "hjosugi",
      "source": "rfcs/0006-module-level-bindings.md",
      "document": "rfcs/0006-module-level-bindings.md",
      "proposal": "https://github.com/hjosugi/kofun/issues/955",
      "dates": {
        "opened_on": "2026-08-05",
        "review_closed_on": "2026-08-09",
        "decided_on": "2026-08-09"
      },
      "normative_spec": [
        "rfcs/0006-module-level-bindings.md"
      ],
      "compatibility": {
        "category": "additive",
        "statement": "Every shape v1 admits is refused today by `E2S02`, so no program that compiles changes meaning and none stops compiling. New surface is the module constant declaration `let NAME = <integer literal>`, the rule that it is not a lexical binding and is therefore shadowed rather than redefined by a function-local `let` of the same name, its lowering to one file-scope `static const int64_t`, and the three codes E2S159, E2S160 and E2S161. Those three sit in a free range: E2S147 belongs to optional-construction, E2S148 through E2S153 to const-generics, E2S154 through E2S157 to the Stage 2 parser, Text and List[Int] slices, and E2S158 to the call-arguments surface, and none is touched. The RFC narrows no production in spec/grammar.ebnf, so no currently-grammatical program becomes ungrammatical.",
        "corpus_query": "git grep -hE '^let ' -- '*.kofun' | wc -l; git grep -lE '^let ' -- '*.kofun' | wc -l; git grep -hE '^let ' -- '*.kofun' | grep -vcE '^let [A-Za-z_][A-Za-z0-9_]* = -?[0-9]+$'",
        "result": "`75`, `23` and `2` at 2b7581ff, the audited commit. Of 75 top-level `let` declarations across 23 tracked `.kofun` sources, 73 are exactly `let NAME = <integer literal>` and are admitted by v1. The 2 exceptions are `let mut LIMIT = 1` and `let LIMIT = 1 + 2`, which are the refusal fixtures this slice adds, so the count of tracked sources outside the new fixtures requiring migration is 0. Restricted to the standard library the same query returns 61 declarations across 15 sources, all of them admitted.",
        "evidence": "tests/module-constants/mutable.stdout"
      }
    },
    {
      "id": "RFC-0007",
      "title": "Derive v1 is a closed compiler-owned set, specified as the expansion a library derive would produce",
      "provenance": "rfc",
      "state": "proposed",
      "shepherd": "hjosugi",
      "source": "rfcs/0007-derive-v1.md",
      "document": "rfcs/0007-derive-v1.md",
      "proposal": "https://github.com/kofun-lang/kofun/issues/988",
      "dates": {
        "opened_on": "2026-08-09"
      },
      "normative_spec": [
        "rfcs/0007-derive-v1.md"
      ],
      "compatibility": {
        "category": "additive",
        "statement": "`@derive` is refused today by tests/security/generated-meta-access.sh as a fail-closed producer boundary, so no program that compiles uses the surface and none can change meaning. New surface is the `@derive(...)` attribute on a `type` declaration, one closed-set member `eq` over concrete nominal records with `Int` or `Bool` fields, the `kofun expand` command, and the five codes E380 through E384. Those sit in a free band: E370 through E372 belong to RFC-0005 and E373 through E379 are unallocated, so nothing is touched. Generated declarations are ordinary declarations, so DD-032 coherence and the orphan rule apply to them unchanged rather than being widened.",
        "corpus_query": "git grep -c '@derive' -- '*.kofun' | wc -l",
        "result": "`0` at 6b7c060f, the audited commit. No tracked Kofun source uses the surface, so no program requires migration.",
        "evidence": "tests/security/generated-meta-access.sh"
      }
    },
    {
      "id": "RFC-0008",
      "title": "Type-level programming v2: general type functions under a versioned fuel profile",
      "provenance": "rfc",
      "state": "accepted",
      "shepherd": "hjosugi",
      "source": "rfcs/0008-type-level-general-v2.md",
      "document": "rfcs/0008-type-level-general-v2.md",
      "proposal": "https://github.com/kofun-lang/kofun/issues/1130",
      "dates": {
        "opened_on": "2026-08-09",
        "review_closed_on": "2026-08-09",
        "decided_on": "2026-08-09"
      },
      "normative_spec": [
        "rfcs/0008-type-level-general-v2.md"
      ],
      "compatibility": {
        "category": "additive",
        "statement": "No program changes meaning and none stops compiling: the active compiler does not parse `type fn`, so neither the existing structural form nor the new `general` modifier exists in any tracked source. New surface is the `general` modifier on a `type fn` declaration, the reduction profile `kofun.type-reduction/general-v2` with its three fixed limits, the tail-replacement and cycle-witness rules that make those limits reproducible, the `kofun.type-reduction-trace/v2` schema as a new sibling name rather than an extension of v1, and the four codes E390 through E393. Those sit in a free band: E380 through E384 belong to RFC-0007, E385 through E389 are unallocated, and E394 through E401 are RFC-0009's. The `kofun.type-reduction/default-v1` profile, its limits, and its trace schema are unchanged, a default-v1 root still emits the v1 trace byte for byte, and a structural declaration may not call a general one, so a v1 root keeps the v1 guarantee compositionally. Trace v2 is defined jointly with RFC-0009 over disjoint field sets, so either proposal may be accepted alone and land the schema carrying only its own contribution. If this proposal is accepted, DD-031's rejection of Turing-complete type programming is superseded for the general class by a recorded ledger amendment rather than by an edit to the decision text.",
        "corpus_query": "git grep -nE '^type fn ' -- '*.kofun' | wc -l",
        "result": "`0` at a654f7fe, the audited commit. No tracked Kofun source declares a type-level function, so no program can observe the new cost class, the new profile, or the new trace schema.",
        "evidence": "spec/type-level-programming-v1.md"
      }
    },
    {
      "id": "RFC-0009",
      "title": "Type-level kinds v1: Nat, Symbol, and Bool as type-level data",
      "provenance": "rfc",
      "state": "accepted",
      "shepherd": "hjosugi",
      "source": "rfcs/0009-type-level-kinds-v1.md",
      "document": "rfcs/0009-type-level-kinds-v1.md",
      "proposal": "https://github.com/kofun-lang/kofun/issues/1133",
      "dates": {
        "opened_on": "2026-08-09",
        "review_closed_on": "2026-08-09",
        "decided_on": "2026-08-09"
      },
      "normative_spec": [
        "rfcs/0009-type-level-kinds-v1.md"
      ],
      "compatibility": {
        "category": "additive",
        "statement": "No program changes meaning and none stops compiling: no tracked source annotates a type parameter with a kind, declares a type-level function, or declares a type whose name this proposal reserves, and `Nat`, `Symbol` and `Bool` become type-namespace names in kind position only, so no existing nominal type is shadowed. New surface is the three data kinds beside `Type`, their literal forms with a 64-bit `Nat` bound and a 65,536-byte `Symbol` bound, erased data-kind parameters on nominal type declarations with equality decided on reduced normal forms and stuck terms compared syntactically, eleven constant-step builtin type functions occupying reserved names, the two kind-directed pattern views `Succ` and `Cons` with binder-only subpatterns, the reduction profile `kofun.type-reduction/kinds-v1` with tail replacement, and the eight codes E394 through E401. Those sit in a free band: E390 through E393 belong to RFC-0008 and E402 onward is unclaimed. Data-kind values and the new `builtin-application` step record are contributed to the `kofun.type-reduction-trace/v2` schema defined jointly with RFC-0008 over disjoint field sets, so either proposal may be accepted alone; no existing sidecar, semantic-event or v1 trace reader gains a field, and a default-v1 root still emits the v1 trace byte for byte. Indices are erased and no term may occupy an index position, so layout, ownership and the refinement, const and shape solvers are untouched.",
        "corpus_query": "git grep -nE '\\[[A-Za-z_]+:[[:space:]]*(Nat|Symbol|Bool)\\b' -- '*.kofun' | wc -l",
        "result": "`0` at a654f7fe, the audited commit. No tracked Kofun source annotates a type parameter with a data kind. Two companion queries return `0` at the same commit: `git grep -nE '^type fn ' -- '*.kofun' | wc -l`, so no declaration can take a data-kind parameter either, and `git grep -nE '^type (Add|Mul|Monus|Div|Mod|NatEq|NatLt|NatLe|Concat|Length|SymbolEq)\\b' -- '*.kofun' | wc -l`, so no tracked type declaration collides with a reserved builtin name.",
        "evidence": "spec/type-level-programming-v1.md"
      }
    },
    {
      "id": "RFC-0010",
      "title": "A resource handle is an affine per-type protocol with a closed transition table",
      "provenance": "rfc",
      "state": "implemented",
      "shepherd": "hjosugi",
      "source": "rfcs/0010-affine-resource-handle-v1.md",
      "document": "rfcs/0010-affine-resource-handle-v1.md",
      "proposal": "https://github.com/kofun-lang/kofun/issues/784",
      "dates": {
        "opened_on": "2026-08-10",
        "review_closed_on": "2026-08-10",
        "decided_on": "2026-08-10"
      },
      "normative_spec": [
        "rfcs/0010-affine-resource-handle-v1.md"
      ],
      "compatibility": {
        "category": "none",
        "statement": "The bounded implementation adds one test-owned `AffineTransport` table and its focused gate; no existing program changes meaning and none stops compiling. A resource handle remains a nominal record carrying a generation token, its transitions are ordinary functions that `take` the handle and return a successor, `read` observes without advancing state, `close` and `cancel` are terminal and return a copyable summary rather than borrowing the consumed handle, and reuse exists exactly where a successor handle does. No new keyword, no `own` domain, and no new compile-time diagnostic are added: the static refusals remain E2S122 and E2S123. The only new identity is the runtime backstop EARH01, executed by the scripted host boundary. `general-ownership-checking` remains open."
      },
      "implementation": {
        "change": "https://github.com/kofun-lang/kofun/issues/784",
        "enablement": "Enabled as one bounded `AffineTransport` table in `task affine-resource-handle`: the standalone record evaluator and Stage 2 C11 execute the same read/write/drain/close/cancel transition bodies, the production move slice refuses three E2S123 misuse shapes, and the scripted C11 host boundary refuses dead or duplicated generations with EARH01. There is no new keyword, `own` domain, environment authority, or general ownership pass.",
        "gate": "task affine-resource-handle",
        "claims": [
          "affine-resource-handle"
        ]
      }
    },
    {
      "id": "RFC-0011",
      "title": "AggregateLayout gains a bounded inline list kind, and Stage 2 record fields use it",
      "provenance": "rfc",
      "state": "accepted",
      "shepherd": "hjosugi",
      "source": "rfcs/0011-bounded-inline-list-layout.md",
      "document": "rfcs/0011-bounded-inline-list-layout.md",
      "proposal": "https://github.com/kofun-lang/kofun/issues/1183",
      "dates": {
        "opened_on": "2026-08-10",
        "review_closed_on": "2026-08-10",
        "decided_on": "2026-08-10"
      },
      "normative_spec": [
        "rfcs/0011-bounded-inline-list-layout.md"
      ],
      "compatibility": {
        "category": "additive",
        "statement": "No tracked program changes meaning and none stops compiling; source the Stage 2 backend refuses today begins to compile, and nothing that compiles today is refused. New surface is the `bounded_list` value kind in AggregateLayout v1 — capacity-generic, inline, `u64` length at offset 0, alignment derived from the element rather than from size, trivial drop when the element bitmap is empty — and the Stage 2 C11 bootstrap profile's mapping of a `List[Int]` record field onto `bounded_list[Int, 64]`. The `list` kind is unchanged and every committed golden vector stays byte-identical, so this is recorded against DD-033 as the amendment `DD-033/A01` rather than as an edit to the layout decision text. No new diagnostic code: `E2S32` keeps the refusal and widens the slice its message names, and every other element type stays refused.",
        "corpus_query": "git grep -nE '^[[:space:]]*[a-z_][A-Za-z0-9_]*:[[:space:]]*List\\[' -- '*.kofun' | wc -l; git grep -nE '^[[:space:]]*[a-z_][A-Za-z0-9_]*:[[:space:]]*List\\[Int\\]' -- '*.kofun' | wc -l",
        "result": "`28` and `19` at 43d53d38, the audited commit, across 1067 tracked `.kofun` sources. Twenty-eight record fields are typed `List[...]`; nineteen of them, across six files, are `List[Int]` and are the ones this proposal admits. The remaining nine name `List[Text]`, `List[LogField]`, `List[Transition]`, `List[IntMapEntry]`, `List[TomlEntry]`, `List[RegexPiece]`, `List[Waiter]` and `List[List[Text]]`, and stay refused.",
        "evidence": "spec/aggregate-layout-v1/layout.mjs"
      }
    },
    {
      "id": "RFC-0012",
      "title": "A raw foreign binding is classified by its source, and crossed once explicitly",
      "provenance": "rfc",
      "state": "accepted",
      "shepherd": "hjosugi",
      "source": "rfcs/0012-raw-foreign-module-trust.md",
      "document": "rfcs/0012-raw-foreign-module-trust.md",
      "proposal": "https://github.com/kofun-lang/kofun/issues/902",
      "dates": {
        "opened_on": "2026-08-10",
        "review_closed_on": "2026-08-10",
        "decided_on": "2026-08-10"
      },
      "normative_spec": [
        "rfcs/0012-raw-foreign-module-trust.md"
      ],
      "compatibility": {
        "category": "additive",
        "statement": "No tracked program changes meaning and none stops compiling. Every construct is new surface: an optional `trust` line on a module header, the single closed-set value `raw-foreign`, the contextual `trusted import` crossing, KIF required tag `0x800A` carrying the class and participating in both semantic digests, and the refusals for ordinary/aliased/selective import, re-export, unknown or unattached trust values, and raw externs in anonymous mode. Absence of the line means ordinary, and no tracked source writes it, so nothing acquires a trust class. The `trust` and `trusted` contextual keywords collide with no tracked source. The one non-source effect is that KIF artifacts rebuild once: the existing rule that a tag with bit `0x8000` set is required already makes an old reader reject `0x800A` with a rebuild instruction rather than misread it, and this proposal requires a new reader to reject an artifact missing it rather than assume safe. DD-025 is not amended: the source `module` header remains the sole module-path authority, and the trust class joins that same authority under the same no-repair-from-path refusals.",
        "corpus_query": "git grep -nE '^[[:space:]]*trust[[:space:]]' -- '*.kofun' | wc -l; git grep -nE '^[[:space:]]*trusted[[:space:]]+import' -- '*.kofun' | wc -l; git ls-files '*.raw.kofun' | wc -l",
        "result": "`0`, `0` and `0` at 1c27baa8, the audited commit, across 1067 tracked `.kofun` sources carrying 109 module headers. No tracked source begins a line with `trust`, none writes `trusted import`, and no generated raw binding is committed, so the two contextual keywords collide with nothing and no tracked module acquires a class.",
        "evidence": "spec/modules/module-identity.md"
      },
      "amendments": [
        {
          "id": "A01",
          "recorded_on": "2026-08-11",
          "change": "https://github.com/kofun-lang/kofun/issues/1212",
          "delta": "Required top-level KIF tag `0x800A` carries exactly canonical UTF-8 `ordinary` or `raw-foreign`. Source still spells only `raw-foreign`; a writer serializes the default ordinary state explicitly. Missing, unknown, and source-contradictory values reject as rebuild-required. This clarifies the active RFC-0012 envelope rather than creating a new version.",
          "original_semantics": "The active envelope requires `0x800A` carrying canonical UTF-8 from a closed set whose only v1 value is `raw-foreign`, while absence of the source `trust` line means the module is ordinary.",
          "compatibility": {
            "category": "none",
            "statement": "No compiler implements RFC-0012 and no tracked KIF artifact carries its top-level `0x800A`, so no program or artifact changes meaning. The amendment makes the previously impossible ordinary writer state representable. Old readers reject the required unknown tag; new readers reject a missing tag and accept only the two exact values, so no artifact is silently reinterpreted."
          }
        }
      ]
    },
    {
      "id": "RFC-0013",
      "title": "Kofun is a self-contained native toolchain, not a hosted-language frontend",
      "provenance": "rfc",
      "state": "accepted",
      "shepherd": "hjosugi",
      "source": "rfcs/0013-self-contained-native-toolchain.md",
      "document": "rfcs/0013-self-contained-native-toolchain.md",
      "proposal": "https://github.com/kofun-lang/kofun/issues/1218",
      "dates": {
        "opened_on": "2026-08-11",
        "review_closed_on": "2026-08-11",
        "decided_on": "2026-08-11"
      },
      "normative_spec": [
        "rfcs/0013-self-contained-native-toolchain.md",
        "spec/native-toolchain-v1/contract.json"
      ],
      "compatibility": {
        "category": "none",
        "statement": "This target contract changes no accepted program and promotes no capability. Existing C11, shell, Node.js, Rust-shim, and go-task paths remain valid bounded bootstrap, test, or interoperability evidence; they simply do not satisfy `kofun-only-native/v1` until the required core build path is implemented entirely by the released Kofun toolchain."
      }
    },
    {
      "id": "RFC-0014",
      "title": "Host operations use explicit bounded authorities",
      "provenance": "rfc",
      "state": "accepted",
      "shepherd": "hjosugi",
      "source": "rfcs/0014-explicit-host-authority-profiles.md",
      "document": "rfcs/0014-explicit-host-authority-profiles.md",
      "proposal": "https://github.com/kofun-lang/kofun/issues/1241",
      "dates": {
        "opened_on": "2026-08-11",
        "review_closed_on": "2026-08-11",
        "decided_on": "2026-08-11"
      },
      "normative_spec": [
        "rfcs/0014-explicit-host-authority-profiles.md",
        "spec/native-toolchain-v1/contract.json"
      ],
      "compatibility": {
        "category": "additive",
        "statement": "Existing zero-argument entrypoints and programs retain their meaning. The opt-in root entrypoint, `pure fn`, compiler-known authority types, process request, and directory enumeration profile are new target surface that no active compiler accepts. No existing host helper is reclassified as a shipped language capability."
      }
    },
    {
      "id": "RFC-0015",
      "title": "Decimal and Fixed keep one value model across direct backends",
      "provenance": "rfc",
      "state": "accepted",
      "shepherd": "hjosugi",
      "source": "rfcs/0015-decimal-fixed-backend-profiles.md",
      "document": "rfcs/0015-decimal-fixed-backend-profiles.md",
      "proposal": "https://github.com/kofun-lang/kofun/issues/1255",
      "dates": {
        "opened_on": "2026-08-11",
        "review_closed_on": "2026-08-11",
        "decided_on": "2026-08-11"
      },
      "normative_spec": [
        "rfcs/0015-decimal-fixed-backend-profiles.md",
        "docs/DECIMAL.md",
        "spec/native-toolchain-v1/contract.json"
      ],
      "compatibility": {
        "category": "additive",
        "statement": "No current compiler implements Decimal-backed `Fixed[S]`, and direct native and Wasm Decimal inputs remain unsupported, so no accepted executable program changes meaning. Existing C11 Decimal v1 values, bytes, limits, and D001-D007 retain their exact meaning. The design-era Fixed example gains an explicit typed-result boundary before it becomes executable."
      }
    },
    {
      "id": "RFC-0016",
      "title": "HTTP/1.1 core uses the generic bounded carrier profile",
      "provenance": "rfc",
      "state": "accepted",
      "shepherd": "hjosugi",
      "source": "rfcs/0016-http-c11-carrier-profile.md",
      "document": "rfcs/0016-http-c11-carrier-profile.md",
      "proposal": "https://github.com/kofun-lang/kofun/issues/1256",
      "dates": {
        "opened_on": "2026-08-11",
        "review_closed_on": "2026-08-11",
        "decided_on": "2026-08-11"
      },
      "normative_spec": [
        "rfcs/0016-http-c11-carrier-profile.md",
        "docs/stdlib/http-client.md",
        "spec/native-toolchain-v1/contract.json"
      ],
      "compatibility": {
        "category": "additive",
        "statement": "The portable HTTP client and its generic carriers are specified but unimplemented, so no accepted program changes meaning. Choosing the ordinary generic Stream avoids introducing and later removing a temporary monomorphic public API; existing server/framework checkpoints and live network capability claims remain unchanged."
      }
    },
    {
      "id": "RFC-0017",
      "title": "Rank-1 generics use static dictionaries, source-free KIF v3, and a small proof kernel",
      "provenance": "rfc",
      "state": "accepted",
      "shepherd": "hjosugi",
      "source": "rfcs/0017-generics-kif-proof-profile.md",
      "document": "rfcs/0017-generics-kif-proof-profile.md",
      "proposal": "https://github.com/kofun-lang/kofun/issues/1265",
      "dates": {
        "opened_on": "2026-08-11",
        "review_closed_on": "2026-08-11",
        "decided_on": "2026-08-11"
      },
      "normative_spec": [
        "rfcs/0017-generics-kif-proof-profile.md",
        "spec/roadmap-31-34/generics-and-traits.md",
        "spec/native-toolchain-v1/contract.json",
        "spec/native-toolchain-v1/schema.json"
      ],
      "compatibility": {
        "category": "additive",
        "statement": "Production paths currently refuse the new generic and trait shapes or stop before backend output. KIF v1/v2, const-generic identities, standalone frontend goldens, the bounded self-host fixed point, and law-evidence v2 remain byte-identical, and no old artifact is reinterpreted as v3."
      }
    },
    {
      "id": "RFC-0013",
      "title": "Bit operations on `Int` are named methods, and the operator symbols stay free",
      "provenance": "rfc",
      "state": "accepted",
      "shepherd": "hjosugi",
      "source": "rfcs/0013-int-bit-operations.md",
      "document": "rfcs/0013-int-bit-operations.md",
      "dates": {
        "opened_on": "2026-08-12",
        "review_closed_on": "2026-08-12",
        "decided_on": "2026-08-12"
      },
      "normative_spec": [
        "rfcs/0013-int-bit-operations.md"
      ],
      "compatibility": {
        "category": "additive",
        "statement": "No tracked program changes meaning and none stops compiling. There is no grammar change: `postfix = primary, { call | member | index }` already admits the member-call form that `handle.join()` uses. No word is reserved — `and`, `or`, `xor`, `not`, `shl`, `shr`, `rotr` and `wrapping_add` stay ordinary identifiers everywhere, and become meaningful only as members of an `Int` receiver. No operator symbol is added: `&`, `^`, `<<` and `>>` are free in the grammar but are left free, because `|` is not — it delimits the `par` scope token and separates ADT variants, so an operator set could cover four of the five and would leave one spelling mixed into every dense expression. `R011` is a new runtime code for a shift count outside `0..63` or a width outside `1..64`; no existing program can reach it, because no existing program can call an operation that raises it.",
        "corpus_query": "git ls-files '*.kofun' | wc -l; git grep -cE '^[[:space:]]*fn (and|or|xor|not|shl|shr|rotr|wrapping_add)\\(' -- '*.kofun' | wc -l; git grep -cE '\\.(and|or|xor|not|shl|shr|rotr|wrapping_add)\\(' -- '*.kofun' | wc -l",
        "result": "`1105`, `0` and `0` at ffa3d8a7, the audited commit. No tracked source declares a function with any of the eight names and none calls a member with them, so no spelling collides and nothing acquires new behaviour. The motivating gap is measured the same way: no tracked `.kofun` source implements a digest, and `bin/kofun-digest` is a shell script that builds `bootstrap/stage2/sha256_tool.c`, so every SHA-256 preimage the compiler is specified to assign — ModuleId, FileId, SymbolId, and scope-HIR v2 ParId/TaskId/JoinId — is computed by C the language cannot express.",
        "evidence": "spec/grammar.ebnf"
      },
      "amendments": [
        {
          "id": "A01",
          "recorded_on": "2026-08-12",
          "change": "https://github.com/kofun-lang/kofun/issues/1350",
          "delta": "Semantics: parameters positional/unlabelled; bit names dispatch only on `Int`; same names elsewhere ordinary. I64 two’s-complement. `shl`: n=0..63 then mathematical `a×2^n` must fit I64; checked; only future `wrapping_shl` may wrap. `shr`: same n, arithmetic. `rotr(n,w)`: w=1..64 first; n=0..63; then n mod w (`64,0` width;`64,32` count;`33,32`=`1,32`). `wrapping_add`: same w. Both reduce mod 2^w; w<64 nonnegative; w=64 maps `2^63→-2^63`,`2^64-1→-1`.\n\nRuntime exact=`error[CODE]: MESSAGE\\n`; no generated-C span: R010 `integer overflow in \\`shl\\``; R011 `shift count for \\`<m>\\` is outside \\`0..63\\`` (m=shl/shr), `rotation count for \\`rotr\\` is outside \\`0..63\\``, `bit width for \\`<m>\\` is outside \\`1..64\\`` (m=rotr/wrapping_add). After operands: rotr width then count; shl count then overflow.\n\nCompile=`error[CODE]: MESSAGE at byte <n>`; E2S168 `unknown \\`Int\\` member \\`<name>\\``/member, `bit operations are defined on \\`Int\\`, and this receiver is \\`<actual>\\``/dot, `\\`<method>\\` takes \\`Int\\` arguments`/bad expression; E2S169 `\\`<method>\\` takes positional arguments; the bit operations have no parameter names`/label, expr `\\`<method>\\` takes no trailing lambda; the bit operations have no functional parameter`/`fn`, `\\`<method>\\` expects <expected> arguments, got <actual>`/method; block body E2S158/`fn`. Order: receiver; each ordinary arg LTR—bind/check its label, then type-check it; expr trailing; arity (`x.not(\"s\")`→type).\n\nOperators: binary arithmetic=`+ - * / // % **`; unary arithmetic=`+ -`; unary Boolean=`!`; other=logic/equality/comparison/range/coalescing/pipeline; no direct bits. Noncrypto digests exist; tracked Kofun SHA-256 absent. IDs=digest outputs over framed preimages. C ModuleId/FileId/SymbolId use `bootstrap/stage2/{semantic_producer,kif_v1,sha256}.c`. `bin/kofun-digest`: C-backed generic digest CLI for repo checksums, reference/gate preimages; not universal production compiler identity. ParId/TaskId/JoinId model `spec/concurrency/scoped-captures-v1/model.mjs`→`node:crypto`; compiler emission absent.",
          "original_semantics": "Kofun cannot compute a digest, and the reason is not that nobody has written the\ncode.\n\nThe language's entire operator set is `+ - * / // % **` and unary `+ - !`\n(`spec/grammar.ebnf:121-124`).\n\nNot one of the\n1105 tracked `.kofun` sources implements a digest, because none can.\n\nEvery identity the compiler assigns\nis a SHA-256 preimage — `ModuleId`, `FileId`, `SymbolId`, and the scope-HIR v2\n`ParId`/`TaskId`/`JoinId` frozen in\n`spec/concurrency/scoped-captures-v1.md`. **The compiler cannot compute the\nidentities it is specified to assign.** #1220 asks it to emit `ParId` and\n`TaskId`; it cannot, and neither can any successor, until this exists.\n\n**`shl` traps on overflow.** `a.shl(n)` traps as `R010` when the result would\nleave signed 64-bit range, exactly as `a * 2**n` does today.\n\n**`width` is `1..64`.** `rotr` and `wrapping_add` trap as `R011` outside it. For\nboth, the value operands are first reduced modulo `2**width`, so a caller\nworking in 32 bits does not have to mask before every call.\n\nTwo runtime codes and no new compile-time family:\n\n- `R010`, the existing integer-overflow trap, covers `shl`.\n- `R011`, new, covers a shift count outside `0..63` and a width outside `1..64`.\n\nA non-`Int` receiver or argument is refused by ordinary type checking with the\nexisting `E2S` arity/type diagnostics; no new code is required for it.\n\n- **result**: `1105`, `0` and `0` at `ffa3d8a7`, the audited commit. No tracked\n  source declares a function with any of the eight names, and none calls a\n  member with them, so no spelling collides and nothing acquires new behaviour.\n\n**Whether `shl` should trap or wrap.** This proposes trapping, consistent with\n`*`. A hash implementation then writes `a.shl(n).wrapping_add(0, 32)` where it\nmeans a 32-bit shift, which is awkward enough that a `wrapping_shl` may be\nwanted. Deferred rather than guessed: the answer should come from writing the\nSHA-256 in step 2 and seeing which form the code actually wants.",
          "compatibility": {
            "category": "additive",
            "statement": "No tracked program at the base declares/calls these unimplemented methods. A01 changes no accepted program; it closes future semantics, diagnostics, evidence and provenance before implementation.",
            "corpus_query": "base=80f79798361c9513a637c521ec03d718d3842dfc; git ls-tree -r --name-only \"$base\" | grep -c '\\.kofun$'; git grep -lE '^[[:space:]]*fn (and|or|xor|not|shl|shr|rotr|wrapping_add)\\(' \"$base\" -- '*.kofun' | wc -l; git grep -lE '\\.(and|or|xor|not|shl|shr|rotr|wrapping_add)\\(' \"$base\" -- '*.kofun' | wc -l; git grep -nEi 'sha.?256|0x428a2f98|0x71374491|1116352408|1899447441' \"$base\" -- '*.kofun' | wc -l; git grep -lE '0x428a2f98|0x71374491|1116352408|1899447441' \"$base\" -- '*.kofun' | wc -l",
            "result": "At base 80f79798361c9513a637c521ec03d718d3842dfc: 1114 tracked .kofun files; 0 declaration files; 0 call files; 11 bounded SHA-256 text rows; 0 first-round-constant files. The 11 rows are one native-fixture comment, two Stage 2 help lines, seven observations_sha256 stored-text accessors, and one usability comment—not a tracked Kofun SHA-256 round implementation.",
            "evidence": "tests/rfc/check-registry.sh"
          }
        }
      ]
    }
  ]
}
